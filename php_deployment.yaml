apiVersion: apps/v1
kind: Deployment
metadata:
  name: php
  labels:
    tier: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: php
      tier: backend
  template:
    metadata:
      labels:
        app: php
        tier: backend
    spec:
      volumes:
      - name: dir
        hostPath:
          path: /mpesasorce
      containers:
      - name: php
        image: kiyange26773/mpesakyawebportal-dev:latest
        volumeMounts:
        - name: dir
          mountPath: /mpesasorce
      initContainers:
      - name: install
        image: busybox
        volumeMounts:
        - name: dir
          mountPath: /mpesasorce
        command:
        - cp
        - "-r"
        - "/var/www/."
        - "/mpesasorce/app"
