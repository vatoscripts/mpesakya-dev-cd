apiVersion: apps/v1
kind: Deployment
metadata:
  name: php
  labels:
    tier: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: php
      tier: backend
  template:
    metadata:
      labels:
        app: php
        tier: backend
    spec:
      volumes:
      - name: dir
        hostPath:
          path: /kya-dev1
      containers:
      - name: php
        image: kiyange26773/mpesakyadevapp:v1.2
        volumeMounts:
        - name: dir
          mountPath: /kya-dev1
      initContainers:
      - name: install
        image: busybox
        volumeMounts:
        - name: dir
          mountPath: /kya-dev1
        command:
        - cp
        - "-r"
        - "/var/www/."
        - "/kya-dev1/app"
