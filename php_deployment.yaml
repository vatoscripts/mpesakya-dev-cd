apiVersion: apps/v1
kind: Deployment
metadata:
  name: php
  labels:
    tier: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: php
      tier: backend
  template:
    metadata:
      labels:
        app: php
        tier: backend
    spec:
      volumes:
      - name: dir
        hostPath:
          path: /kya
      containers:
      - name: php
        image: kiyange26773/mpesakyadevapp
        volumeMounts:
        - name: dir
          mountPath: /kya
      initContainers:
      - name: install
        image: busybox
        volumeMounts:
        - name: dir
          mountPath: /kya
        command:
        - cp
        - "-r"
        - "/var/www/."
        - "/kya/app"
