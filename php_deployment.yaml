apiVersion: apps/v1
kind: Deployment
metadata:
  name: php
  labels:
    tier: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: php
      tier: backend
  template:
    metadata:
      labels:
        app: php
        tier: backend
    spec:
      volumes:
      - name: dir
        hostPath:
          path: /kya-dev
      containers:
      - name: php
        image: kiyange26773/mpesakyadevapp:v1.1
        volumeMounts:
        - name: dir
          mountPath: /kya-dev
      initContainers:
      - name: install
        image: busybox
        volumeMounts:
        - name: dir
          mountPath: /kya-dev
        command:
        - cp
        - "-r"
        - "/var/www/."
        - "/kya-dev/app"
