apiVersion: apps/v1
kind: Deployment
metadata:
  name: php
  labels:
    tier: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: php
      tier: backend
  template:
    metadata:
      labels:
        app: php
        tier: backend
    spec:
      volumes:
      - name: dir
        hostPath:
          path: /kya-dev2
      containers:
      - name: php
        image: kiyange26773/mpesakyadevapp:v1.3
        volumeMounts:
        - name: dir
          mountPath: /kya-dev2
      initContainers:
      - name: install
        image: busybox
        volumeMounts:
        - name: dir
          mountPath: /kya-dev2
        command:
        - cp
        - "-r"
        - "/var/www/."
        - "/kya-dev2/app"
